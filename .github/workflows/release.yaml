name: Release

on:
  push:
    tags:
      - '*'

jobs:
  docs:
    name: 'Build and deploy documentation'
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.pages-url }}

    permissions:
      contents: read
      pages: write
      id-token: write

    concurrency:
      group: pages
      cancel-in-progress: true

    steps:
      - name: 'Checkout'
        uses: eliashaeussler/gha/.github/actions/checkout@6f449e3c6a24edf70fd6e69945a8be1b2d8f8c4c # 1.1.4

      - name: 'Build docs'
        env:
          COMPOSE_FILE: build/docs/docker-compose.yaml
        run: |
          docker-compose build --pull
          docker-compose run --rm docs build

      - name: 'Deploy docs'
        id: deploy
        uses: eliashaeussler/gha/.github/actions/deploy-pages@6f449e3c6a24edf70fd6e69945a8be1b2d8f8c4c # 1.1.4
        with:
          base-path: 'site'

  release:
    name: 'Create release'
    uses: eliashaeussler/gha/.github/workflows/github-release.yaml@6f449e3c6a24edf70fd6e69945a8be1b2d8f8c4c # 1.1.4
    permissions:
      contents: write
